<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:clickable="true"
    android:background="#ccc"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
  <RelativeLayout
    android:background="@drawable/back"
    android:layout_width="match_parent"
    android:layout_height="300dp">
    <com.pkmmte.view.CircularImageView
      android:layout_marginTop="30dp"
      android:layout_marginLeft="16dp"
      android:id="@+id/sideNavAvatar"
      android:src="@drawable/logo"
      android:layout_height="100dp"
      android:layout_width="100dp"/>
    <TextView
      android:textColor="#fff"
      android:textStyle="bold"
      android:layout_marginTop="100dp"
      android:layout_marginLeft="16dp"
      android:text="kdkdkdk"
      android:textSize="24sp"
      android:layout_below="@id/sideNavAvatar"
      android:id="@+id/userfirstnameforsidenav"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"/>

    <TextView
      android:textColor="#fff"
      android:layout_marginTop="100dp"
      android:textSize="24sp"
      android:text="dkdkllskdkjd"
      android:layout_marginLeft="8dp"
      android:layout_toRightOf="@id/userfirstnameforsidenav"
      android:layout_below="@id/sideNavAvatar"
      android:id="@+id/userlastnameforsidenav"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"/>
  </RelativeLayout>

  <ListView
    android:divider="@null"
    android:id="@+id/sideNavList"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
  </ListView>

</LinearLayout>